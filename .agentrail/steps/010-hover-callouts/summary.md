Step 10 done: hover callouts. IR symbols carry docs (u:rs_ir_symbol_doc, u:rs_ir_undocumented, docs in equation.json; all 17 one-neuron symbols documented); scene panels (u:rs_scene_panel: id/title/doc/bounds/color; Visual face names dataflow/gradients/data/parameters; page legend built from them); page callout box + leader/arrowhead for [data-id] symbols (glyph, name, doc, role, value at epoch), [data-panel] hit rects, [data-doc] chrome via CHROME_DOCS; focusin/taps; Esc; Hints toggle; ?hover=ID. Chrome quirk: getBoundingClientRect of SVG children in a 3D-rotated face is mirrored, so pointer callouts anchor at the pointer and stills map bbox via the SVG root. Cross-section rotated so Math normal = 0. Footer with © / MIT / GitHub / build facts from build-site (pages/data/build-info.json). Baselines rebased (equation, scene). 32 tests, just check green. User asked mid-step for a stone dropdown (goes in 013 multi-stone-page: use a <select>) and the footer (done).