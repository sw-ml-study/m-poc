The fourth verb, Zoom: show me what is inside (research §40, semantic zoom). Scope for v1.1: one level. (1) lib/rosetta/ir.mlpl: a record may attach an EXPANSION to a symbol: another equation record (same shape: symbols, docs, Math and M faces) that says what that symbol is made of; exported under symbols.expansions keyed by id; validation that an expansion's own symbols are documented. (2) Expansions for the tour: one-neuron's grad_w expands to the chain rule ∂L/∂w = ∂L/∂ŷ · ∂ŷ/∂w = 2(ŷ−y) · x (M: ∇W L ← 2×(Ŷ−Y)×X), the CNN's win expands to the index arithmetic X[q, x+u, y+v] over the window, softmax's exp to the series or leave it; at least two expansions. (3) Page: key z (and a Zoom button in the Focus row) on a focused symbol that has an expansion opens a flat 'inside' panel in front of the stone showing the expansion's Math and M faces rendered by the same faces pipeline (fixtures/<stone>/faces/<symbol>-math.svg etc., emitted by faces.mlpl for every expansion), with callouts and focus working inside it; Esc or z zooms out; the face card notes 'inside: …' for symbols that can be zoomed; a breadcrumb shows stone › symbol. (4) docs/plan.md: Zoom moves from deferred to v1.1 with this scope; README: the verb. Tests and baselines for the expansions. just check green; commit to main; push.