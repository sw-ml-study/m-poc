Attention stone: Math S_ij = (Q Kᵀ)_ij / √d, P_ij = σ(S_i)_j, A_ik = ∑_j P_ij V_jk with per-index tokens; M face S ← (Q +.× ⍉K) ÷ √d, P ← σ S, A ← P +.× V composed from the matmul and softmax glyphs; full symbol, line and face docs. demos/attention/{equation,compute,train,faces,visual,scene}.mlpl: n=3, d=2, S/P/A via matmul, transpose, softmax, sqrt; trace axis 'query' with i, S row, P so far [3,3,3], A row; tests by hand and against the builtins (weight rows sum to one, A rows equal the weighted value rows). Visual: dataflow composition, Q/K/V grids with the current query row lit, score and weight bars, A grid filling in. Manifest ninth (after sigmoid, per the plan's variant order rather than the prompt's 'seventh', which predates the image stone); check-structure; README; three baselines; stones baseline rebased; 66 tests. Screenshot-checked all three faces. just check green; committed and pushed to main.