Hover callouts act dead on a face turned to the front with keys 1/2/3 (reported for the M face, key 2, on the sigmoid stone). Diagnosed with a DevTools-protocol sweep over yaw (tmp/ev.mjs pattern): document.elementFromPoint at a front face's centre returns the #stone box or the #stage instead of the face for whole bands of yaw: the M face for yaw −100..−30 (key 2 lands at −85.5) and the Visual face for yaw 60..95; the Math face only when it is edge-on. Blink's 3D hit testing loses the faces when the stone's own box (the z=0 plane of the preserve-3d context, which the M and Visual faces cross) is between or nearly edge-on. Fix in pages/style.css and pages/rosetta.js: put the yaw rotation on the faces (rotateY(yaw + a)) so the stone's own plane always faces the viewer, and make the stone's box transparent to the pointer (pointer-events: none on .stone, auto on .face); keep the exact-anchor projection (originMatrix reads computed transforms, so it follows). Verify with the sweep that every front-facing face is hit at every yaw on every stone, and by eye. Bump ?v= on the page assets. just check green; commit to main; push.