Hover fix: the yaw rotation moved from the #stone element onto the faces (rotateY(yaw + a)) and the stone's box made pointer-events: none (faces auto). Cause, found with a DevTools-protocol sweep (tests/hover-sweep.mjs, kept): Blink's 3D hit testing returned the stone's own box or the stage instead of a front-facing face whenever the stone's z=0 plane (which the M and Visual faces cross) lay between the viewer and the face or was nearly edge-on; the M face was dead for yaw −100..−30 including the key-2 target −85.5, the Visual face for yaw 60..95. Verified: old CSS 19/27 views failing on sigmoid, new CSS 0/27 on all eight stones; callouts screenshot-checked front and oblique. v=30, README status. just check green; committed and pushed to main.