The second stone in the tour, the simplest sum: a dot product / weighted sum, s = ∑_{i=1}^{n} w_i x_i (Math face with a stacked ∑ and subscripts; per-index tokens so i, n, w, x, s are all symbols), M face in the provisional notation 'S ← +/ W×X' (say the glyphs are placeholders). demos/dot-product/: equation.mlpl (the record, with the face docs: a named title and purpose per face, a legend for the Visual panels, and a one-sentence doc per symbol for the hover callouts, all in the record so the page reads them), compute.mlpl (real numbers: n = 5 values of w and x computed by MLPL; the trace's time axis is the index i, count n+1: per step the term w_i x_i, the running sum, and the finished s; keyed by symbol ids; compare the final sum with reduce_add and with matmul as an oracle), visual.mlpl (the Visual face: the two vectors as labelled cells side by side, the current pair highlighted, the products row, and the running-sum bar growing; dataflow boxes W, X → × → +/ → S; frames per step), faces.mlpl and scene.mlpl scripts writing fixtures/dot-product/ and the manifest entry (fixtures/stones.json). Tests: the sum matches the oracles, ids are symbols, every face and symbol has a doc, fixtures fresh; reg-rs baselines. Page: the stone appears in the selector; the face card shows the named face labels from the record; hovering any token on the equation faces shows its callout (the hover-callouts step must already work; if it does not yet read per-stone docs, make it). README: the stone in the tour list. just check green; commit to main; push.