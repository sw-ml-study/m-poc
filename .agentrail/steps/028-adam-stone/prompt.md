The eighth stone: Adam on the one-neuron loss, showing why the research wants tuples (§21). Math face, four lines: m ← β₁m + (1−β₁)g, v ← β₂v + (1−β₂)g², ŵ… use m̂ = m/(1−β₁ᵗ), v̂ = v/(1−β₂ᵗ), w ← w − η m̂/(√v̂ + ε), with g = ∂L/∂w from the one-neuron loss (real grad; include b the same way or state that b is held at its trained value: choose the simplest honest option and document it); M face with the research's placeholder step glyph ↻ and a state tuple, e.g. (M V) ← ↻ (M V) G; full docs, including one for the state tuple. demos/adam/: compute with grad and the update rules (check against sw-MLPL's adam builtin if one exists; otherwise against a closed-form step); time = epochs (~30); channels m, v, m̂, v̂, w, L, g; Visual: the parameter path of Adam next to plain gradient descent from the same start (two paths in the w–b or w–L plane), and bars for m and v; dataflow boxes. Manifest eighth; baselines; check-structure; README. just check green; commit to main; push.