Step 25 done: sigmoid stone derived from one-neuron (σ symbol on both faces, sigmoid on the tape, data ±0.5/±1.5 with targets 0,0,1,1, η=2, 40 epochs; loss 0.25→0.016 monotone; Visual with σ box and sampled S curve). Manifest sixth; baselines rosetta_sigmoid_{trace,faces,scene}; stones rebased; 60 tests; just check green. Saga note: agentrail insert panics when a multibyte char (§) sits at the description's byte-77 boundary; keep the first ~90 bytes of a prompt ASCII.