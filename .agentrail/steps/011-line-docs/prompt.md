Per-line explanations on the text faces. Extend lib/rosetta/ir.mlpl so each line of a face can carry a one-sentence doc (u:rs_ir_line_doc(ir, face, line_index, doc), stored pipe-delimited like the rest; exported per face as 'line_docs' in equation.json; a validation helper naming the first undocumented line). In lib/rosetta/faces.mlpl tag each line's <text> element with data-line=INDEX so the page can find it. Document the one-neuron lines on both faces (e.g. Math line 0: 'The prediction: the neuron's guess for y is a straight line through x with slope w and intercept b'; line 1: 'The loss: how far the guess is from the target, squared'; lines 2 and 3: 'The update: move w (b) a small step against its gradient so the loss falls next epoch'). Regenerate fixtures and rebase the baselines deliberately. Page: resting the pointer on a line's empty area (the <text> element, outside a token) shows the line's callout; a token's callout gains a last line 'line N: <line doc>'; the face card lists the lines with their docs under the face purpose. Tests: line docs round-trip, undocumented line named, one-neuron lines all documented, data-line present on every text element. README: how to read a line. just check green; commit to main; push.