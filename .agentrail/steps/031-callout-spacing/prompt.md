Callouts obscure each other (reported). Cause: layout() in pages/rosetta.js stacks tiers above the row and clamps each tier's top to the viewport, so when the room above is short the tiers collapse onto one another; the overall (line/panel) callout also shares the first tier with glyphs. Rework: the overall callout gets its own tier, always leftmost (at the row's left) and the highest of the fan; glyph tiers spread with wider gaps; the side is chosen by where the whole fan fits, and tiers that do not fit above spill below the row instead of overlapping (the overall stays on top). Verify with headless-Chrome screenshots at a short viewport and a tall one; bump ?v=; just check; commit to main; push.