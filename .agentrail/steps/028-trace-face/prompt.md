A fourth face, the Trace: the stone becomes a box for stones that declare it. (1) lib/rosetta: a stone may carry a fourth scene, trace-scene, a line scene of the time axis itself: the loss (or the stone's headline scalar: L for the neuron, sigmoid and Adam stones; s for the dot product; the current output cell for the CNN; skip stones with no headline scalar) over the steps with a moving marker at the current step; emitted by demos/<stone>/trace-scene.mlpl into fixtures/<stone>/trace-scene.json for the stones that have one (at least one-neuron, sigmoid, adam). (2) The manifest gains a per-stone list of faces (math, m, visual, and trace when present) and the widths for four faces. (3) The page's geometry generalises from a triangle to a tangential polygon: faces tangent to one incircle; for four faces a rectangle-like kite (widths must satisfy a + c = b + d, else fall back to equal widths with a console error); the minimap draws the real polygon; Rotate gets a fourth button and key 4 when the stone has a Trace face; read mode, focus, callouts and the projector work on the fourth face (a second projector instance). (4) The page copies fixtures per the manifest; build-site/check-site/check-fixtures walk the extra file when present. Tests, baselines, docs/stones.md and scene.md, README. just check green; commit to main; push.