Callout fan reworked in pages/rosetta.js: select() keeps one overall callout (nearest line or panel); layout() gives it its own tier at the row's left, always the highest; glyph boxes to its right with GAP 22 / TIER_GAP 30; the side is chosen by fit (above, else below with the overall nearest the row, else overall plus fitting tiers above and the rest below); tiers that would leave the viewport are hidden, never piled. Verified by headless-Chrome screenshots at 900×780 and 1280×1100 on the sigmoid M face and the image-normalization and CNN Visual faces: no box covers another, the overall is top-left. hover-sweep passes; v=31; README. just check green; committed and pushed to main.