Callouts in expression order (pages/rosetta.js). Reported: with the fan alternating above and below the row, a callout for the rightmost part of an expression can appear left of one for an earlier part, which confuses reading. Fix: in proximity mode place every glyph callout on ONE side of the row (above when there is room, else below), ordered strictly left to right by the anchor's x, laid out like text: a first tier next to the row, and when the next box would run past the viewport a further tier that continues the order, never restarting at the left in a way that puts a later glyph left of an earlier one within the same tier; leaders must not cross (if a box's leader would cross a neighbour's, shift the box). The overall (whole-line or panel) callout stays leftmost and larger; the nearest glyph keeps its emphasis. Also cap the fan at four glyph callouts plus the overall one so the tiers stay shallow. Verify with ?near= stills on the one-neuron Math face (line 3) and the CNN Math face, checking that reading the boxes left to right, top to bottom, follows the expression. README: one clause. just check green; commit to main; push.