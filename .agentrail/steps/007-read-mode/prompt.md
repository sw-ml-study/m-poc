Legibility on the stone page (pages/, no build step). (1) Cache-busting: version queries on the style.css and rosetta.js links in index.html so a changed asset is never reused from cache (the user once saw a stale, cropped M face). (2) Read mode / zoom: double-click (or a Read button / Enter) on a face brings that face flat and enlarged in front of the stone, filling the viewport height with the whole face content fitted (never cropped), still live (scrubber keeps working, tokens still clickable for focus); Escape, click outside, or a Reset-view button returns to the stone at the default size. A +/- zoom for the stone itself with a Fit/Reset control. (3) Orientation cues: a small face indicator (three chips lit by which face is toward the viewer, or a tiny top-down minimap of the prism with the camera) so the reader always knows which face they are seeing and that faces have more content than the current view. Keep the projector and faces generic (ids only). Verify with headless Chrome at desktop and narrow widths, including that all four lines of each text face are visible in every mode; update README (Rotate/Zoom verbs; note that Zoom in the plan's sense, abstraction levels, is still deferred); just check green; commit to main; push.