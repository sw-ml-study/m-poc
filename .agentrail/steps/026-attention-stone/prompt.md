The seventh stone: attention, composed from the matmul and softmax stones (research §9, R10). Three lines: S = QKᵀ/√d, P = softmax(S), A = PV, with Q, K, V small (n = 3 tokens, d = 2), on the Math face with subscripts where needed (per-index tokens for i, j; √d as a token); M face in the provisional notation, e.g. S ← (Q +.× ⍉K) ÷ √d, P ← σ S (softmax placeholder from the softmax stone), A ← P +.× V; full docs. demos/attention/: compute with MLPL's matmul, transpose, softmax; the trace's time axis is the query position i (count n): per step the score row S_i, the probability row P_i, and the output row A_i, plus the whole P so far; keyed by symbol ids; tests against the builtins (P rows sum to one, A = P·V exactly). Visual: Q, K, V as labelled grids, the current query row lit, the score row and probability row as bars, the output row filling in; dataflow boxes Q, K → +.× → ÷√d → σ → +.× ← V → A. Manifest seventh; baselines; check-structure; README. just check green; commit to main; push.