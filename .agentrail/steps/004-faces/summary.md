Step 4 done: lib/rosetta/faces.mlpl renders Math and M faces via svg(text, "equation") and wraps every token in <tspan class=rs-token data-id=SYMBOL> (fails closed if the renderer's line text would change or a token holds markup); lib/rosetta/scene.mlpl is a demo-free line-scene builder (vertices, edges with id+RGBA, polylines, labels, frames per axis step; validation; export as sw-ml-study.native3d.line-scene + ids/colors/labels/frames, rank-2 as shape+values). demos/one-neuron/visual.mlpl builds the Visual face from the parsed trace: dataflow boxes with gradient back-paths, data plot with fitted line and residuals, parameter-space path with update arrow; 31 frames, 143 vertices, 118 edges, 15 labels. Fixtures: faces/math.svg, faces/m.svg, scene.json; 12 new tests (28 total), check-fixtures covers all four fixtures, baselines rosetta_one_neuron_faces and rosetta_one_neuron_scene, docs/scene.md, README. just check green. Commit on main.