IR and faces v2, for equations with sums and indices (lib/rosetta/ir.mlpl, lib/rosetta/faces.mlpl, tests, baselines). Tokens may now carry (1) a subscript/superscript in the renderer's own syntax (e.g. text 'x_i', '^2', '∑_{q=1}^{Q}') and (2) index groups written as per-index tokens (W ~[ r ~, q ... ~]) so each index symbol is focusable. Study what svg(text, "equation") emits for such lines (it splits a line into several <text> elements: plain runs, a centered ∑ with stacked limit texts, tspans with baseline-shift for sub/superscripts) and rewrite u:rs_face_svg to walk the renderer's runs in order, matching them against the face's tokens so every token's glyphs are wrapped in tspans (or the run's <text>) carrying data-id, still changing no character of the layout, failing closed on any mismatch. The tests pin: the stripped text equals the face text; every token has a data-id; the one-neuron faces are byte-identical to before (no regression); a new tiny record with ∑_{i=1}^{n} x_i and a superscript renders and tags correctly; reg-rs baseline for the tiny record. Keep lib demo-free. docs/scene.md faces section updated. just check green; commit to main; push.