Data cache-busting: rosetta.js reads the ?v= of its own script tag (document.currentScript) and appends it to every data fetch (manifest, fixtures, build-info), so bumping the version in index.html refreshes the data as well; GitHub Pages' max-age=600 had let a browser pair the new script with the old manifest and drop the Trace face (reported: no 4 on the live demo; it cleared by itself after ten minutes). README notes the rule; v=34. just check green; committed and pushed.