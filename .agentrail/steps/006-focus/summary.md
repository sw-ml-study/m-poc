Step 6 done: focus in pages/rosetta.js. Any [data-id] element (text-face tspans, Visual edges and labels, readout rows) is a target; clicking focuses that id everywhere (.focus on matches, body.has-focus dims the rest, CSS in style.css), caption shows glyph/name/role from equation.json and the value at the current epoch (channel row, constant, or 'an operator'); Esc, Clear, second click end it; ?focus=ID for stills; drags no longer count as clicks. Also added ?v=6 cache-busting on the style/script links (part of step 7's prompt, done early because stale CSS misled the user). Verified with headless Chrome at three views. just check green. NOTE for step 7: the user prefers the name 'view scale' over 'zoom' for the magnification control; keep 'Zoom' for the plan's abstraction-level verb.