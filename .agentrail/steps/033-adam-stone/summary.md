Adam stone: Math face six scalar lines (g = ∂L/∂w, m, v, the two corrections, the w step, the same for b); M face six tuple lines (Θ ← (W B), G ← ∇Θ L, (M V) ← ↻ (M V) G with the placeholder step glyph, M̂, V̂, Θ ← Θ − η×M̂ ÷ √V̂ + ε); full docs including the state tuple. demos/adam/ reuses the one-neuron loss and params; Adam as explicit update rules with grad, 30 epochs at the first stone's rate; trace channels t, w, b, L, g, m, v, mhat, vhat (pairs). Tests: every row obeys the rules, first step moves w by η, the adam builtin lands on the same parameters, loss falls tenfold, the plain path shares start/rate/epochs. Visual: dataflow with the state loop, four moment bar pairs, the (w, b) plane with Adam's path beside plain descent's read from fixtures/one-neuron/trace.json. Manifest tenth; check-structure; README; three baselines; stones baseline rebased; 68 tests; screenshot-checked. just check green; committed and pushed to main.