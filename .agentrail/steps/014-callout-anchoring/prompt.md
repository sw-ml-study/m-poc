Callouts must stay anchored to the glyph they explain (pages/rosetta.js, style.css). Reported: when the stone moves (auto-rotate, drag, g grab, r rotate, s scale, read mode, view scale, resize) a shown callout keeps its old box position and its leader drifts to a random nearby glyph; and the fan is jumbled. Fixes: (1) the shown set is a list of ELEMENTS, and their anchors are recomputed from those same elements on every change of the view: hook the stone's onChange (yaw/pitch/scale/pan/read) and a requestAnimationFrame loop while anything is shown, and on scroll/resize; leaders and boxes follow the anchors (boxes slide with their anchor; the tier layout is recomputed from the new anchors) so a callout never points at a different glyph than the one it was made for; (2) hysteresis: the near set is recomputed only when the pointer has moved more than ~24px from where the set was made or has left the row/face; small motion and stone motion re-anchor without re-selecting; while a modal key mode is armed or a drag is in progress, the set is frozen and only re-anchored; (3) spacing: larger gaps between boxes (≥16px) and between tiers and the row (≥24px), at most five callouts, boxes on one side spread out so leaders stay short and do not cross, and a box is never placed over another callout's anchor; (4) each callout's leader ends exactly on its glyph's box (the per-character extent), with the arrowhead touching the glyph's edge nearest the box. Verify with headless stills using ?near=ID combined with ?yaw= and ?scale= (the anchors must land on the same glyphs at any yaw/scale) and a still after a simulated view change (add ?then=yaw:DEG applied 300ms after the callouts are shown). README: one line under callouts. just check green; commit to main; push.