Give each face of the stone its own width: a scalene triangular prism (pages/, no build step). Face widths from content: Visual about 720px, Math and M about 480px (scaled together by the viewport, e.g. --unit = min(1, 92vw/720)); the prism height stays one value for all faces. Geometry: the cross-section is the triangle whose side lengths are the three face widths (check the triangle inequality; fail loudly in the console if violated); place every face tangent to the inscribed circle: translateZ(r) with r the inradius, and rotateY by that side's outward-normal angle computed from the triangle (no more fixed 0/120/240). Compute the angles and r in JS from the widths and set them as CSS variables per face; FACE_ANGLE, turnTo, frontFace and the read-mode fit must use the computed per-face angles and widths (read mode grows that face's own width). The minimap draws the real triangle (same side lengths, same orientation) so the shape reads as the stone's. The Visual face's projector fits the wider box; the text faces keep all four lines visible. Verify with headless Chrome at desktop and narrow widths, in default, read and scaled views; update README (stone geometry paragraph) and docs if any contract mentions the prism; just check green; commit to main; push.