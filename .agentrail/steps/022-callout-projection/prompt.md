Three fixes to the page's faces and callouts (pages/rosetta.js, style.css, index.html). (1) Face headers: just the face name and the stone's title as the dropdown shows it ('Math · CNN triple sum'), never the equation or the notation, which the face itself shows; the M face keeps its 'proposed notation · placeholder glyphs' note in small text. (2) Exact anchors on the rotated stone: anchors are currently mapped linearly through the SVG root's client rect, which is only right when the face is flat (read mode); on a turned face the leaders miss their glyphs. Compute the projection exactly: compose the DOMMatrix chain the CSS applies (the SVG's placement and viewBox scale inside the face, the face's transform about its transform-origin, the face's offset in the stone, the stone's transform about its origin, the stage's perspective(1800px) about its perspective-origin), project the four corners of a glyph's box (per-character extents) with the perspective divide, and use their bounding box in client coordinates; keep the linear path only as a fallback when a matrix cannot be read. Verify with stills at yaw ±40 and ±100 using ?near= and ?then=yaw: that every leader ends on its glyph. (3) Layout of a fan: the whole-line callout (and, when present, the face or panel callout) is the 'overall' one: larger type, a distinct border, placed leftmost above the row (or below if no room), always to the left of and visually separate from the glyph callouts, which fan to its right; the nearest glyph keeps its emphasis. README: one line. just check green; commit to main; push.