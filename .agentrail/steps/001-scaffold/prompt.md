Step 1 of docs/plan.md: scaffold the repository. Mirror ../demo-decision-model conventions: justfile (default lists tasks; check, tests, reg, serve-site), scripts/select-mlpl and scripts/select-mlplunit (copy from ../demo-decision-model/scripts, absolute-path rules unchanged), scripts/run-tests, scripts/run-reg (sets REG_RS_DATA_DIR=tests/reg relative to the repo root and runs reg-rs run -p rosetta), scripts/check (sh -n on scripts, check-mlpl-style, check-abstraction for lib/ vs demo vocabulary, run-tests, run-reg), mlplunit.conf (source_root ., test_root tests, pattern test_*.mlpl), .github/workflows/pages.yml publishing pages/ as is, directories lib/rosetta demos/one-neuron fixtures pages tests tests/reg. Add one smoke mlplunit test (tests/test_smoke.mlpl) and one reg-rs baseline (mlpl-repl --version filtered to the version line) so just check runs end to end and passes. Update README Status. Run just check, commit to main, push.