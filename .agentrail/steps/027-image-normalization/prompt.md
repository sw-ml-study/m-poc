The second applicative stone, from docs/research2.txt ('Then evolve the same demo to rank 2'): image normalization, the fixed tuple lifted over an image by broadcasting as an index projection. Math face: z[h,w,c] = (x[h,w,c] − μ[c]) / σ[c] with per-index tokens, and a second line 'h ∈ Fin(4), w ∈ Fin(4), c ∈ Fin(3)' (the index space as a product); M face: 'Z ← (X − Μ) ÷ Σ' and a second line 'X : Tuple[4,4,3]  Μ Σ : Tuple[3]' (the shapes; placeholders), with docs saying the tuple μ is never materialized to the image's shape: broadcasting is the index map (h,w,c) ↦ c. demos/image-normalization/: compute.mlpl (a tiny 4×4×3 image X computed by MLPL, e.g. a ramp per channel plus a bright spot, the RGB mean and std tuples from the feature stone, z by trailing-axis broadcasting; checked against an explicit loop), train.mlpl (the trace: axis 'pixel' with 16 steps in row-major order; per step the pixel's three channel values, μ, σ, and z for that pixel, the current h and w, and z so far), faces.mlpl, visual.mlpl/scene.mlpl (three channel grids of x with the current pixel outlined, the μ and σ tuples as three cells each drawn ONCE with a leader to the current pixel, the three output grids filling in one pixel per step, and a panel doc about non-materialized broadcasting), tests (z equals the loop, broadcasting shape [4,4,3], keys are symbols, docs complete, fixtures fresh), baselines, check-structure, manifest after softmax and before cnn, README tour entry. just check green; commit to main; push.