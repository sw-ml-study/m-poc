command = """sh -c "./scripts/run-mlpl demos/cnn/faces.mlpl | grep -v '^viz: '""""
exit_code = 0
desc = "The CNN stone's Math face (the triple sum with every index a token) and M face (the window glyph and a reduce over named axes), tagged."
