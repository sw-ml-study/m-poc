Step 8 done: scalene prism. FACE_WIDTHS {visual 720, m 480, math 480}, height 440, --unit px/unit; makeGeometry computes the triangle with those sides, inradius r, per-face outward-normal angle and in-plane offset (side midpoint vs tangent point), sets CSS vars (--r, --stone-w, --face-hu on the stone; --w, --a, --o per face); FACE_ANGLE = -angle; read mode fits --unit to the read face and zeroes --r/--o inline (inline beats stylesheet: earlier stylesheet override was silently ignored); minimap draws the real cross-section. Verified at 1280/520 in default, turned, read views; autoplay confirmed (epoch advances). just check green. User mid-step reported 'Visual face stopped animating': current build autoplays; likely a mid-edit load or a paused state; asked to hard-reload.