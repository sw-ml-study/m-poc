Callouts fail for a moment after changing faces (reported). Cause: the stone's transform has CSS transitions (.stone.settling after turnTo, and body.reading .stone always); the exact-anchor measurement toggles the transform off and on each frame, and with a transition active the switch-back animates, so anchors are computed mid-animation and glyphs seem out of reach. Fix: remove those transform transitions (custom-property changes never animated anyway) and, in measureFlat, also force transition: none inline during the toggle as a belt-and-braces; verify with a still that uses ?near= together with ?then=yaw: (a turn) and one in read mode; README unchanged. just check green; commit to main; push.