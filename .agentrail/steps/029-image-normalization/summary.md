Image-normalization stone: Math z[h,w,c] = (x[h,w,c] − μ[c]) / σ[c] with the product index space h ∈ Fin(4), w ∈ Fin(4), c ∈ Fin(3); M face Z ← (X − Μ) ÷ Σ unchanged from the feature stone plus the shapes X : Tuple[4,4,3] Μ Σ : Tuple[3], docs on non-materialized broadcasting as the index map (h,w,c) ↦ c. demos/image-normalization/{equation,compute,train,faces,visual,scene}.mlpl: 4×4×3 ramp-plus-spot image built by MLPL, z by trailing-axis broadcasting checked against three explicit loops; trace axis pixel (16, row-major) with h, w, x, sub, div and z-so-far [16,4,4,3]; Visual face with three channel grids, μ/σ drawn once with a leader from the current pixel, three output grids filling in, index-projection panel. Tests (2, 64 total), three reg-rs baselines, manifest after softmax before cnn with widths 620/560/800, check-structure, README tour entry and status. just check green; committed 4e8988a to main and pushed. Verified by headless-Chrome screenshots of the Visual, Math and turned stone. Note: docs/research2.txt is still untracked though README and docs/plan.md link to it. Reported mid-step: M-face (pane 2) callouts act dead on the sigmoid stone; not reproduced (Chrome extension disconnected), cause not found by reading pages/rosetta.js; candidate for an inserted step.