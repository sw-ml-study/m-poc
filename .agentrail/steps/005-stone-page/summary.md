Step 5 done: pages/ static stone. index.html/style.css/rosetta.js: CSS 3D triangular prism with the Math and M SVGs inlined (token tspans are DOM nodes) and the Visual face; auto-rotate, pointer drag, Rotate buttons; generic line projector (orthographic camera fitted over all frames, one <line> per edge with color and data-id, labels) replaying scene.json frames; epoch scrubber with play/pause and a readout of every trace channel labelled from equation.json (new fixture emitted by faces.mlpl). scripts/build-site copies fixtures to pages/data; scripts/check-site verifies the copy in the gate; ?yaw=&epoch= URL params for stills. Verified with headless Chrome screenshots (claude-in-chrome extension was not connected). just check green (28 tests, 5 baselines). README: page section + status.