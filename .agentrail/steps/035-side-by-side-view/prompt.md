An alternate flat view of the stone (requested): the faces side by side in landscape, Math | M | Visual (and Trace when the stone has one), each pane live (the projector, focus, callouts, the scrubber and the readout keep working), toggled from the View row ('Side by side') and by a key, with ?view=flat for stills, and leaving it returns to the stone at the view you had (like read mode, which it generalises: read mode is one pane, this is all of them). Plus a step-through, 'Walk': a control (and keys n / p or ←/→ while walking) that steps through the record in expression order, line by line and token by token, highlighting the current line and then each token on every pane at once (the Math and M tspans by id, the Visual face's edges and labels by id) and showing that token's doc and value as a caption; ?walk=N for stills. Works on the stone too, not only flat. Page only (pages/, no MLPL change); docs; README; just check green; commit to main; push.

Toggle (asked 2026-09-25): more than one way in and out of the wide view: a "Side by side" button in the View row, the w key, and ?view=wide in the URL; the same button or key, or Esc, returns to the rotating stone at the view you had. Not Tab (browsers use it for keyboard focus).
