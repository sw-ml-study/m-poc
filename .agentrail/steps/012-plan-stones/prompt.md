Planning step: update docs/plan.md (the authority for scope) for what the user asked on 2026-09-23: (a) the page steps that were inserted after step 5 (read mode/view scale/face card, scalene prism, modal keys, hover callouts) become part of the plan's step table and the verb table (Rotate, Travel, Focus, plus View: magnification, keys, callouts; Zoom in the abstraction sense stays deferred); (b) two more stones, no longer optional: matmul C ← A +.× B with time = the contraction index k (research §61), and the CNN triple sum y[r,x,y] = ∑_q ∑_u ∑_v W[r,q,u,v]·X[q,x+u,y+v] (Zhao et al. 2018 §2.1) as sw-MLPL says it: windows → kernel × patches → reduce over channel,kernel_y,kernel_x, with time = the output position the window slides over; sources are ../sw-mlpl/examples/literate/cnn-convolution.org and the blog post ../../software-wrighter-lab/blog/_posts/2026-09-10-ml-cnn-from-equations-mlpl.md (link both from the plan and, later, the page); (c) what the IR and faces need for those: subscripts/superscripts and stacked ∑ limits as tokens, multiple text runs per line from the renderer, index groups like W[r,q,u,v] as per-index tokens, and the research's placeholder glyphs ⧉ (window) and multi-axis +/ reduce on the M face, all still provisional; (d) a multi-stone page: a manifest of stones, per-stone fixture directories, a stone selector, per-stone face docs from each record. Also record the open decision of a fourth 'Trace' face (loss over epochs) if the stone ever becomes a box. Update README status. No code. just check green; commit to main; push.