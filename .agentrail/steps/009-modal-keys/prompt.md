Blender-style modal keyboard controls for the stone page (pages/rosetta.js, no build step), building on step 007's view scale, read mode and face indicator. Press a key to enter a mode, move the mouse (or use arrow keys) to act, click or Enter to confirm, Esc to cancel and restore the previous state: g = grab (translate/pan the stone in the stage), r = rotate (mouse x -> yaw, mouse y -> a small pitch), s = scale (view scale, mouse y), t = travel (mouse x scrubs the epoch; left/right arrows step one epoch), f = focus (then a click picks the symbol; f again or Esc clears). 1/2/3 turn to the Math, M and Visual faces; Home (and 0) resets view scale, pan and yaw to the default; Space toggles play/pause; a = toggle auto-rotate. Show a small on-screen key legend (like Blender's header hint) that names the active mode, and a '?' overlay listing the keys. Keys must not fire while typing in an input. Keep everything generic (no neuron knowledge). Verify with headless Chrome using URL params where needed; document the keys in README under the verbs; just check green; commit to main; push.