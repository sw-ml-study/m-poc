Auto-rotation shows the faces in the order 3, 2, 1 (reported). Reverse the spin so the stone presents Math, then M, then Visual: negate SPEED in makeStone (and make the default yaw start just before the Math face so the first thing seen is Math turning toward the viewer); dragging keeps its direction (pointer right turns the stone right). Verify with three stills at t = 0, ~8 s and ~16 s of auto-rotation (use ?epoch= to freeze time and virtual-time budgets) that the front face goes Math → M → Visual. README: one clause. just check green; commit to main; push.