Callouts that are easy to hit and can show several things at once (pages/rosetta.js, style.css). Problems reported: a token is hard to land on, and most of the time the whole-face callout ('The Math face') wins because the face section is the hover target. Fixes: (1) the face's own callout is attached to its header (the h2) only, never to the face body; (2) tokens get generous hit areas: pointer-events: bounding-box on tspans/text in the text faces, and a padded invisible hit rect behind each label and each edge group in the Visual face; (3) proximity mode: while the pointer is over a face, gather every target whose anchor lies within a radius (~90px, tuned) of the pointer, using anchors computed through the SVG root's rect (getExtentOfChar over the tspan's character range for tokens; getBBox for lines, labels, panels) because client rects of SVG children in a 3D-rotated face are mirrored in Chrome; show up to about six callouts at once, one per target, fanned around the pointer in distinct directions (sort by bearing from the pointer, assign slots around a ring at increasing distance, push boxes apart so none overlap and none covers a target), each with its own leader line; the nearest target's callout is emphasised, the line's callout (from the line-docs step) sits with its row, and the face callout, when shown, sits furthest away near the header. Moving the pointer down a row drops the previous row's callouts and adds the next row's. Keyboard focus and ?hover=ID keep showing a single callout. Hints toggle still turns everything off. Verify with headless stills using ?hover= and a new ?near=X,Y (viewport point) parameter that runs proximity mode at that point; README (how to read a face). just check green; commit to main; push.