The third stone: the CNN triple sum, one convolutional layer as the paper writes it and as sw-MLPL says it. Sources: ../sw-mlpl/examples/literate/cnn-convolution.org (u:conv_im2col, windows/reshape/matmul, the conv2d oracle) and the blog post ../../software-wrighter-lab/blog/_posts/2026-09-10-ml-cnn-from-equations-mlpl.md (windows, trailing-axis broadcasting, multi-axis and named-axis reduce, the @formula conv_layer); link both from the page's face card and README. demos/cnn/equation.mlpl: Math face 'y[r,x,y] = ∑_{q=1}^{Q} ∑_{u=1}^{Mw} ∑_{v=1}^{Nw} W[r,q,u,v] · X[q, x+u, y+v]' with per-index tokens (r,x,y,q,u,v are symbols; Q, Mw, Nw are hypers); M face in the provisional notation, e.g. 'Y ← +/[channel,kernel_y,kernel_x] W × ⧉[Mw,Nw] X' using the research's placeholder glyphs ⧉ (window) and +/ (reduce over named axes); say so on the face. demos/cnn/compute.mlpl: real numbers from MLPL: a small input (one channel, 6×6 ramp or a tiny pattern) and a 3×3 vertical-edge kernel, the output via windows → kernel × patches → reduce(:add, ..., [2,3,4]), checked against conv2d and against u:conv_im2col; the trace's time axis is the output position (x, y) in row-major order (count = out_y·out_x): per step the receptive field (the 3×3 patch), the products, the running/partial sum over the 9 terms and the finished output value, keyed by symbol ids. demos/cnn/visual.mlpl: the input grid with the sliding window outlined at step t, the kernel grid, the elementwise products grid, and the output grid filling in cell by cell (finished cells lit), plus the dataflow boxes X → ⧉ → × ← W → +/ → Y; frames per step. Tests: output equals conv2d exactly; window positions match; ids are symbols; fixtures fresh; baselines. Page: the stone appears in the selector with its face docs. just check green; commit to main; push.