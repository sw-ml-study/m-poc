Step 15 done: faces v2. IR token modes (0 plain,1 sub,2 super,3 bigop,4 lower,5 upper) via spec markers _ ^ ! < > after the id; rs_face_text emits renderer source with _{ } ^{ } runs; faces.mlpl rewritten: elements grouped by baseline, runs matched to tokens, per-token tspans with the renderer's shifts, centred bigop/limits get inner tspans, lower limit deferred while the upper is matched, data-line on every element, fail-closed on mismatch. One-neuron faces byte-identical. tests/sums_record.mlpl + tests/render_sums.mlpl; baseline rosetta_sums_faces; equation baseline rebased (modes). 36 tests; just check green. Learned: to_number returns a Result; a bare ∑ is always centred by the renderer (M face uses +/); a plain run may end with a trailing space before a centred element.