Trace face: lib/rosetta/timeline.mlpl (u:rs_timeline_scene: axes, the series polyline, a moving marker, min/max labels, one panel); demos/{one-neuron,sigmoid,adam,dot-product,cnn}/trace-scene.mlpl emit fixtures/<stone>/trace-scene.json; the manifest gains faces per stone and widths.trace (math + visual = m + trace for the kite). Page: makeGeometry is now a tangential polygon (tangent lengths, inradius by bisection, three sides reproduce the old triangle exactly, four need a + c = b + d else equal widths with a console error); the Trace section, Rotate button, minimap edge and key 4 exist only when the stone has the face; second projector; face card from the trace scene's panel; callouts search both scenes. Scripts walk trace-scene.json when present; check-structure; tests (timeline lib, fixtures keyed by the headline id, manifest faces and kite); 5 baselines; docs/stones.md, docs/scene.md, README; v=32. Hover sweep 0 failures on one-neuron and cnn; screenshots checked. just check green (70 tests, 39 baselines); committed and pushed to main. Mid-step the user asked for a side-by-side landscape view with a step-through: inserted as step 035 (its prompt.md carries the toggle requirement: a View-row button, the w key, ?view=wide, Esc back).