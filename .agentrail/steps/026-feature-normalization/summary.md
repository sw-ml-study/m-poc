Step 26 done: feature-normalization stone (demos/feature-normalization: two-line faces incl. i ∈ Fin(3) and the Tuple[3] reading; fn.mlpl pointwise steps; lanes + index-space Visual; tests; baselines). Manifest re-ordered to tour order (neuron, feature-normalization, dot-product, matmul, softmax, cnn, sigmoid); plan tour table updated with research2 as source; README tour renumbered. Callout fix (user report): RADIUS 130, nearest line always included on a face body, flatCache reset per selection. 62 tests; just check green.