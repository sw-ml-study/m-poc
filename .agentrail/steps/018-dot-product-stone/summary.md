Step 18 done: dot-product stone (demos/dot-product: equation with full docs, dot.mlpl steps, train/faces/visual/scene; fixtures/dot-product; manifest entry; baselines rosetta_dot_product_{trace,faces,scene}; rosetta_stones rebased). IR validation relaxed to 'every symbol on some face' (bug fixed: boolean sum must use eq(...,0)). Generic scene helpers in lib (rs_points2d, rs_scene_segment/rect/diamond/box). Page: axis name from the trace, plainMath for headers/lines, stone-agnostic lede. 44 tests; just check green. Dropdown now has two stones.