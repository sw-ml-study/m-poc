The fourth stone in the tour, the research's front-page equation: softmax, σ(z)_i = e^{z_i} / ∑_{j=1}^{n} e^{z_j} (Math face with a fraction if the renderer supports one, else the slash form; subscripts and a stacked ∑; symbols: z, i, j, n, e/exp, sigma), M face in the provisional notation, e.g. 'P ← (*Z) ÷ +/ *Z' using the research's placeholder glyphs (say so). demos/softmax/: equation.mlpl (the record with named face titles and purposes, a Visual legend, and a one-sentence doc per symbol for hover callouts), compute.mlpl (real numbers from MLPL's exp and reduce_add over a small logit vector, n = 5, checked against the softmax builtin; the trace's time axis is the temperature T sweeping from high to low, count ~21: per step the scaled logits z/T, the exponentials, the normaliser, and the probabilities; keyed by symbol ids), visual.mlpl (the Visual face: the logits as a bar row, the exponentials as a bar row, the normaliser as one bar, the probabilities as a bar row that always sums to one; dataflow boxes Z → exp → ÷ ← +/ → P; frames per temperature step), faces.mlpl and scene.mlpl scripts writing fixtures/softmax/ and the manifest entry. Tests: probabilities sum to one at every step and match the builtin, ids are symbols, every face and symbol has a doc, fixtures fresh; baselines. Page: in the selector, named face labels from the record, hover callouts on every equation token. README: the stone in the tour list. just check green; commit to main; push.