Side by side (wide) view and Walk, page only. Wide: body.wide lays every face flat in a row across the viewport (stone.setWide: saves the view, fits --unit to the row, min 0.45 px/unit with sideways scroll), from the View-row button, key w, or ?view=wide; button, w or Esc returns to the stone at the saved view; read mode and reset leave it; anchors accept 'none' transforms. Walk: makeWalk steps the record in Math-face order, each line then each symbol once; line steps mark .walk on both text faces' tokens and every [data-id] of the line on the other faces, symbol steps use focus.set with the line kept lit; caption with docs and value; Walk row (Prev/counter/Next/Stop), keys n/p, Esc, ?walk=N; refreshes with the scrubber. Also fixed the reported turn order on four-faced stones (math, trace, visual, m): the polygon walk now goes the old triangle's way (verified identical angles, offsets, vertices) so the order is math, m, visual, trace. README, help, legend, chrome docs; v=33. Screenshot-checked at 900, 1400 and 1920 px; hover sweep 0 failures; just check green (70 tests, 39 baselines); committed 2dbcba1 and pushed to main (Pages deploys on push).