Step 21 done: CNN stone (demos/cnn: equation with full docs incl. the y/y disambiguation (out vs py), cn.mlpl windows→multiply→reduce checked against conv2d and im2col, 16 positions with window/products/sum/filled map; train/faces/visual/scene; fixtures/cnn; manifest fifth with widths math 720/m 600/visual 780; baselines rosetta_cnn_{trace,faces,scene}; rosetta_stones rebased). faces.mlpl: space-only runs between centred elements consume no tokens; trailing-space check guarded. M-face axis names use hyphens (renderer reads _ as subscript). 57 tests; just check green. Dropdown has all five stones.