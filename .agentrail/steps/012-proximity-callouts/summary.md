Step 12 done: proximity callouts. makeCallouts rewritten: per-token anchors via getExtentOfChar mapped through the SVG root (Chrome mirrors client rects of SVG children in 3D-rotated faces); pointer-events: bounding-box on text-face tspans; wide invisible hit twins behind Visual edges and padded rects behind labels; face doc on the h2 only; showNear gathers targets within 90px (max 6), dedupes Visual ids (label beats edge), keeps the hovered row clear and lays boxes in tiers above/below sorted by anchor x; nearest emphasised, others clamped; single callouts for chrome/keyboard; ?near=ID stills. Verified on Math, M, Visual in read mode and on the stone. just check green.