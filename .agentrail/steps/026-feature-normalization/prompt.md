The applicative stone, from docs/research2.txt (the section 'so how can we add an interesting demo to this using these applicative functors'): feature-wise normalization over a fixed index space, with and without homogeneous tuples. Math face, two lines: z_i = (x_i − μ_i) / σ_i (per-index tokens; μ and σ as symbols mu and sigma, the division a div symbol) and 'i ∈ Fin(3)' (the index space, with Fin a hyper symbol documented as the finite index type). M face, two lines: 'Z ← (X − Μ) ÷ Σ' (the array-only reading; placeholders) and 'X Μ Σ Z : Tuple[3]' (the tuple-typed reading; a 'tuple' symbol documented: what fixed arity buys, arity known, one index space, no runtime length check, result arity 3, and that the surface expression is unchanged, which is the experiment's point). Face docs say this is the pointwise/applicative stone: every position computes independently, no reduction, in contrast to the dot product. demos/feature-normalization/: compute.mlpl (x = (120, 80, 200), mean = (123, 117, 104), std = (58, 57, 57), the RGB normalization constants from the notes; z from MLPL's elementwise ops, checked pointwise), train.mlpl (the trace: axis 'position' with 3 steps, per step the current x_i, μ_i, σ_i, the difference and z_i, plus z so far; constants x, mu, sigma; keys are symbol ids), faces.mlpl, visual.mlpl/scene.mlpl (three independent lanes x_i, μ_i → − → ÷ σ_i → z_i with the current lane lit, and an index-space panel: 0,1,2 ──▶ (x, μ, σ) ──f──▶ z with the panel doc saying why this is applicative), tests (z matches (x−mean)/std, keys are symbols, docs complete, fixtures fresh), baselines, check-structure, README tour entry. Also re-order the manifest (the dropdown) simplest to most complex as agreed: one-neuron, feature-normalization, dot-product, matmul, softmax, cnn, sigmoid (update test_stones and rebase rosetta_stones). docs/plan.md: add the two applicative stones to the tour table and note research2.txt as their source. just check green; commit to main; push.