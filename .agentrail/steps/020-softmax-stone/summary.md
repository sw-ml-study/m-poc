Step 20 done: softmax stone (demos/softmax: equation with full docs, sm.mlpl sweep over 21 temperatures with exp/reduce_add checked against the softmax builtin, train/faces/visual/scene; fixtures/softmax; manifest fourth; baselines rosetta_softmax_{trace,faces,scene}; rosetta_stones rebased). Trace axis gains optional per-step values; page shows them (Travel row, callouts). Renderer note: no nested sub in super, so Unicode subscript letters are used inside exponents as their own tokens. 52 tests; just check green. Dropdown has four stones.