Hover callouts that explain each and every thing on the stone page. (1) Content from the record, not the page: extend lib/rosetta/ir.mlpl symbols with a one-sentence 'doc' per symbol (kept beside name and role; validated non-empty, no delimiter; exported in equation.json), fill it in demos/one-neuron/equation.mlpl for all seventeen symbols (e.g. yhat: 'the prediction: what the neuron thinks y is for this x; the hat means estimate'), and give the Visual face's panels and the scene labels docs too (a labels 'docs' list or a per-panel record in scene.json via lib/rosetta/scene.mlpl), regenerating fixtures, tests and reg-rs baselines (rebase deliberately). (2) A static dictionary in pages/rosetta.js for the page's own chrome: each face, each verb row, each button, the scrubber, the readout, the key legend. (3) The callout: hovering (or keyboard-focusing, or tapping on touch) any [data-id] element, control, face title or panel shows one 2D-styled callout box in page space with a leader line and arrowhead pointing at the hovered element's bounding box (getBoundingClientRect works through the 3D transforms); it names the thing, gives its doc, and for symbols its role and value at the current epoch; it flips to stay inside the viewport; a short delay avoids flicker; Esc hides; a 'hints' toggle turns callouts off. Never cover the hovered element. Keep the projector and focus generic (ids only); the callout reads docs by id. Verify with headless Chrome (add ?hover=ID for stills); README under Focus; docs/trace.md or scene.md for the new fields; just check green; commit to main; push.