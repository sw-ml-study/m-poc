Planning complete. Wrote docs/plan.md: a mock-up of a 4D Rosetta Stone (Math / proposed M / Visual faces, epochs as time) for one neuron trained by gradient descent; visualization slice of docs/research.txt only, M code proposed never run, all executing code is sw-MLPL on the native mlpl-repl (svg(text,"equation") renders the glyph faces, grad produces the trace), static HTML/CSS/JS page with no build toolchain. Verified mlpl-repl 0.22.0 renders the target Unicode Math and M strings to SVG and trains the neuron with grad. Bootstrapped the rosetta-m saga, wrote project rules into CLAUDE.md and AGENTS.md, copied COPYRIGHT and LICENSE, wrote the README stub. Committed 3a54bf5.