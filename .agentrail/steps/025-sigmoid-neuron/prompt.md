The sixth stone: the sigmoid neuron, the research's own one-neuron example (section 37 of docs/research.txt): y-hat = σ(wx + b), L = (y − ŷ)², w ← w − η ∂L/∂w, b ← b − η ∂L/∂b, trained by gradient descent with grad through sigmoid on four points whose targets are 0, 0, 1, 1 (a logistic fit; choose x so the fit is visible in ~40 epochs). demos/sigmoid/: equation.mlpl (Math and M faces; M: Ŷ ← σ B + W×X with σ a placeholder function glyph and the rest as the one-neuron stone; full symbol, line and face docs; a 'sigma' symbol documented as the squashing function), neuron.mlpl/train.mlpl (real numbers: param leaves, grad, per-epoch w, b, ŷ, L, gradients; tests: loss monotone non-increasing and ending low, grad vs the closed form ∂L/∂w = mean(2(ŷ−y)ŷ(1−ŷ)x)), visual.mlpl/scene.mlpl (like the one-neuron Visual face, but the fitted curve is the sigmoid sampled at ~24 points as a polyline; the dataflow has a σ box between + and ŷ), faces.mlpl; manifest sixth; baselines; check-structure; README tour entry. It tests whether the stone survives a function glyph. just check green; commit to main; push.