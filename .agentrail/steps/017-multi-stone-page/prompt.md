Several stones on one page (fixtures/, scripts/, pages/). Layout: fixtures/<stone>/{trace.json,scene.json,faces/{math.svg,m.svg,equation.json}} for each stone and a manifest fixtures/stones.json emitted by MLPL (demos/stones.mlpl: id, title, one-line purpose from each record's description, face widths hint) — the equation record gains optional per-face docs (title, purpose, legend entries for the Visual panels) so the page's face card reads from the record rather than a page dictionary (move the one-neuron FACE_DOCS text into demos/one-neuron/equation.mlpl or a companion docs record; keep lib demo-free). scripts/build-site copies every stone; check-site and check-fixtures iterate the manifest; check-structure lists the manifest. pages/: a stone selector (buttons or a select in the header, ?stone=ID), loading data/<stone>/...; the stone rebuilds its faces, the projector and readout re-init, focus clears; face widths per stone from the manifest hint (the scalene prism step's geometry). One stone still (one-neuron) but everything is per-stone. Verify with headless Chrome; README (stones section); just check green; commit to main; push.