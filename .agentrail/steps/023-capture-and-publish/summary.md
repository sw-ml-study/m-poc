Step 23 done: scripts/capture-site (headless Chrome stills per stone + animated WebP per Visual face; POSIX sh, run with sh since the loop's word splitting broke under zsh); captures/ committed (5 stills, 5 WebPs, a WebP of the neuron still for the README); README: hero captures, captures section, run-it-yourself section (mlpl.softwarewrighter.com playground + just mlpl), final status (v1 complete) and what comes next; plan step table marked done; check-structure lists captures and the script. Pages deploy verified live (dropdown, v=22 assets, stones.json with five ids). just check green. User feedback received mid-step: callout fan should be in expression order (one side, left to right); queued as the next step.